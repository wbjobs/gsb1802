{
  "name": "csp-violation-monitor",
  "version": "1.0.0",
  "description": "Capture, aggregate, analyze and export CSP securitypolicyviolation events (SecurityPolicyViolationEvent + IndexedDB + Web Worker)",
  "type": "module",
  "scripts": {
    "start": "node scripts/server.js",
    "test": "node --test test/*.test.js"
  },
  "license": "MIT"
}
